import Header from "@/components/Header";

export default function Login() {
  return (
    <main>
      {/* TO DO: ADD Auth Layout, so that we do not call Header again. */}
      <Header />
      <div>Add logic for login</div>
    </main>
  );
}
